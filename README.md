# Testing bun monorepo support

**HMR not working**

- Clone
- In the root, run `bun install`
- Start `cd packages/web && bun dev`
- Edit `packages/shared/ui/TestComponent.jsx` 
- The component does not update in the browser http://localhost:3000


